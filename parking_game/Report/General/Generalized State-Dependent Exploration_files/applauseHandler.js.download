const isLoggedIn=;const applauseButton=document.querySelector('applause-button');const tooltip=document.createElement('div');tooltip.classList.add('applause-tooltip');tooltip.innerText='You must be logged in to upvote';document.body.appendChild(tooltip);function showLoginPrompt(){tooltip.style.opacity='1';tooltip.style.visibility='visible';setTimeout(()=>{tooltip.style.opacity='0';tooltip.style.visibility='hidden';},3000);}
applauseButton.addEventListener('clapped',()=>{if(!isLoggedIn){const buttonRect=applauseButton.getBoundingClientRect();tooltip.style.left=`${buttonRect.left+(buttonRect.width/2)-(tooltip.offsetWidth/2)}px`;tooltip.style.top=`${buttonRect.top-tooltip.offsetHeight-10}px`;showLoginPrompt();}else{const userId=;sendClapToBackend(userId);}});